/**
 * Resolved hex value for the brand primary colour.
 * Used anywhere a CSS variable cannot be used: native <input type="color">,
 * inline style fallbacks, and database-stored default colours.
 * Must stay in sync with --color-brand-500 in assets/css/main.css.
 */
export const BRAND_HEX = '#6366F1'
