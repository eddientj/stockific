export default defineAppConfig({
  ui: {
    colors: {
      primary: 'brand',
      neutral: 'slate',
      success: 'success',
      warning: 'warning',
      error:   'error',
      info:    'info',
    },

    button: {
      defaultVariants: { color: 'primary' },
    },

  },
})
